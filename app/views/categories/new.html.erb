<div class="container mt-5">
  <h1>Add a new category</h1>
  <%= simple_form_for @category do |f| %>
    <%= f.input :name, label: false, placeholder: "breakfast..." %>
    <%= f.submit class: "btn btn-info" %>
  <% end %>
</div>
